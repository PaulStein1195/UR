import 'package:flutter/material.dart';

class DataCenter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text( " LIVE/ DASHBOARD/ QUESTIONS "));
  }
}
